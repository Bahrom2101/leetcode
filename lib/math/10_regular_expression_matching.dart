void main(List<String> args) {
  print(isMatch('aasv', '.*'));
}

bool isMatch(String s, String p) {
  bool match = true;
  String first = s[0];

  for (int i = 0; i < s.length; i++) {
    if(first != s[i]) {

    }
  }
  return match;
}
